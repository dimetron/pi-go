[project]
name = "hello-world"
version = "0.1.0"
description = "Simple hello world example for LSP testing"
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/hello_world"]
