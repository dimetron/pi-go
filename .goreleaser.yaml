version: 2

before:
  hooks:
    - go mod tidy

builds:
  - main: ./cmd/pi
    binary: pi
    env:
      - CGO_ENABLED=0
      # Enables gomlx's SIMD matmul kernels (Go 1.26 simd/archsimd), which are
      # gated on `amd64 && goexperiment.simd`. Speeds up `pi memory mine` in the
      # released amd64 binaries; inert on the arm64 ones, which have no NEON path.
      - GOEXPERIMENT=simd
    ldflags:
      - -s -w
      - -X github.com/dimetron/pi-go/internal/cli.Version={{.Version}}
      - -X github.com/dimetron/pi-go/internal/cli.BuildTag={{.ShortCommit}}
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64

archives:
  - format: tar.gz
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: checksums.txt

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
      - "^chore:"
      - Merge pull request
      - Merge branch

release:
  github:
    owner: dimetron
    name: pi-go
  draft: false
  prerelease: auto
  name_template: "v{{.Version}}"
